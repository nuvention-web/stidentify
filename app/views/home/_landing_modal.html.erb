
<!-- Modal -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog initial-signup">
    <div class="modal-content">
      <div class="modal-body">

        <%= form_for @initial_user do |f| %>

          <div class="form-group">
            <%= f.text_field :name, placeholder: "name", class: "form-control" %>
          </div>

          <div class="form-group">
            <%= f.label :category, "I am a:" %><br>
              <%= f.radio_button :category, "Coach" %>
              <%= f.label :category, "Coach", class: "radio-inline initial-radio" %>
         
              <%= f.radio_button :category, "Debater" %>
              <%= f.label :category, "Debater", class: "radio-inline initial-radio" %>
         
              <%= f.radio_button :category, "Other" %>
              <%= f.label :category, "Other", class: "radio-inline initial-radio" %>
          </div>
         

          <div class="form-group">
            <%= f.text_field :email, placeholder: "email address", class: "form-control" %>
          </div>

          <div class="form-group">
            <%= f.submit "Submit", :class => "btn btn-new form-control" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
<!-- End Modal -->